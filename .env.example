# See https://nextjs.org/docs/app/building-your-application/configuring/environment-variables

# Required for contact form + submissions list — use your own rotated credentials.
MONGODB_URI=

# Optional: database name override (omit to use Atlas default DB from connection string).
# MONGODB_DB_NAME=vatmaster
